%section.section.workshops-section
  .workshops-header-wrapper
    .workshops-header
      %h1.title Workshops

      .description
        %p We organiseren educatieve en culturele workshops binnen verschillende disciplines, zowel in huis als op aanvraag.

        %p
          %strong Workshops in huis
          worden op vaste tijdstippen door Artuur ingericht, iedereen is vrij om zich hiervoor in te schrijven en deel te nemen!
          %br
          %strong Workshops op aanvraag
          reserveer je op voorhand en dienen enkel voor grotere groepen.

        %p Maak hieronder je keuze en ontdek ons aanbod!

      .type-filters
        %button.type-filter{data: {filter: "type-all"}} Allemaal
        %button.type-filter{data: {filter: "type-in-house"}} In huis
        %button.type-filter{data: {filter: "type-on-request"}} Op aanvraag

      .tag-filters.filter-group
        .subtitle Soort workshop
        - @tags.each do |tag|
          %button.filter-item{data: {filter: "tag-#{tag.id}"}}= tag.name

      .audience-filters.filter-group
        .subtitle Doelgroep
        - @audiences.each do |audience|
          %button.filter-item{data: {filter: "audience-#{audience.id}"}}= audience.name.dasherize


  .workshops-list
    - @workshops.each do |workshop|
      = link_to workshop_url(workshop.id), {class: workshop_class(workshop)} do
        = image_tag workshop.cover_image.url(:medium), class: 'cover-image'
        %h3.title= workshop.title
        .tags
          .tags-wrapper
            - tags = workshop.tags + workshop.audiences
            - tags.each do |tag|
              .tag-item= tag.name.upcase

